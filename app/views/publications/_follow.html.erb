<%= form_for(current_user.relationships.build(followed_id: @publication.id), remote: true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Im Author of this Publication", class: "btn btn-large btn-primary" %>
<% end %>
<%= form_for(current_user.relationships.find_by_followed_id(@publication),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "I make mistake", class: "btn btn-large" %>
<% end %>